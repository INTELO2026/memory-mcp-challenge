mcp>=1.0.0
tiktoken>=0.7.0
numpy>=1.26
google-generativeai>=0.8.0
python-dotenv>=1.0.0

# Dev (CI installe aussi via pyproject)
pytest>=8.0.0
pytest-asyncio>=0.24.0
ruff>=0.8.0

# Fallback local optionnel (hors-ligne / sans clé) — lourd (torch) :
#   pip install -e ".[local]"
# sentence-transformers>=2.2.0
